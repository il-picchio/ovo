import 'package:flutter/material.dart';

class WealthAnalysisSkipModal extends StatelessWidget {
  const WealthAnalysisSkipModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}